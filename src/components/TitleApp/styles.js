import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  header: {
    width: "100%",
    height: 100,
    alignItems: "center",
    justifyContent: "center",
    marginTop: 30,
  },

  title: {
    fontSize: 45,
    fontWeight: "bold",
    color: "#fff",
  },
});

export { styles };
